# Lombok 이 만든 메서드(@Getter·@Builder 등)에 @lombok.Generated 를 붙인다.
# JaCoCo 는 이 애노테이션이 붙은 메서드를 집계에서 자동으로 뺀다. 덕분에 entity·dto 패키지를
# 통째로 제외하지 않고도 생성 코드가 커버리지를 왜곡하지 않으며, 그 안에 직접 쓴 로직
# (금액 계산, 상태 전이, 입력 경계 검사)은 집계에 남는다.
config.stopBubbling = true
lombok.addLombokGeneratedAnnotation = true
