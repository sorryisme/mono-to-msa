<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sorryisme.fmarket.mapper.OrderMapper">

    <resultMap id="orderMap" type="com.sorryisme.fmarket.dto.response.OrderResponseDto">
        <id property="id" column="id" />
        <result property="userId" column="user_id" />
        <result property="orderDate" column="order_date" />
        <result property="status" column="status" />
        <result property="totalAmount" column="total_amount" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />

        <collection property="orderDetails" ofType="com.sorryisme.fmarket.dto.response.OrderDetailResponseDto">
            <id property="id" column="detail_id" />
            <result property="productOptionId" column="product_option_id" />
            <result property="quantity" column="quantity" />
            <result property="price" column="price" />
            <result property="createdAt" column="detail_created_at" />
            <result property="updatedAt" column="detail_updated_at" />
        </collection>
    </resultMap>

    <select id="findAllOrderList" resultType="com.sorryisme.fmarket.domain.Order" parameterType="com.sorryisme.fmarket.dto.request.OrderSearchDto">
        SELECT
            id,
            status,
            order_date,
            total_amount,
            created_at,
            updated_at
        FROM
            `ORDER`
        WHERE user_id = #{userId}
        <if test='startPeriod != null and startPeriod != "" and endPeriod != null and endPeriod != ""'>
            AND order_date BETWEEN #{startPeriod} AND #{endPeriod}
        </if>
        LIMIT #{pageable.pageSize} OFFSET #{pageable.offset}
    </select>

    <select id="countOrderList" resultType="int" parameterType="com.sorryisme.fmarket.dto.request.OrderSearchDto">
        SELECT COUNT(*) FROM `ORDER` WHERE user_id = #{userId}
        <if test='startPeriod != null and startPeriod != "" and endPeriod != null and endPeriod != ""'>
            AND order_date BETWEEN #{startPeriod} AND #{endPeriod}
        </if>
    </select>

    <select id="isExistOrderById" resultType="boolean">
        SELECT EXISTS (SELECT id FROM `ORDER` WHERE id = #{orderId})
    </select>

    <update id="updateOrder">
        UPDATE
            `ORDER`
        SET
           status = #{status}
        where
            id = #{orderId}
    </update>

    <select id="findOrderById" resultMap="orderMap">
        SELECT
            o.id,
            o.user_id,
            o.status,
            o.total_amount,
            o.order_date,
            o.created_at,
            o.updated_at,
            od.id AS detail_id,
            od.product_option_id,
            od.quantity,
            od.price,
            od.created_at AS detail_created_at,
            od.updated_at AS detail_updated_at
        FROM `ORDER` o
             LEFT JOIN `ORDER_DETAIL` od ON (o.id = od.order_id)
        WHERE o.id = #{orderId}
    </select>

    <insert id="createOrder" parameterType="com.sorryisme.fmarket.domain.Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `ORDER`(user_id, order_date, status, total_amount)
        VALUES (#{userId}, now(), #{status}, #{totalAmount})
    </insert>

    <insert id="createOrderDetail" parameterType="com.sorryisme.fmarket.domain.OrderDetail">
        INSERT INTO `ORDER_DETAIL`(order_id, product_option_id, quantity, price)
        VALUES
        <foreach collection="list" item="orderDetail" separator="," >
            (#{orderDetail.orderId}, #{orderDetail.productOptionId}, #{orderDetail.quantity}, #{orderDetail.price})
        </foreach>
    </insert>

    <select id="findOrderByIdForUpdate" resultMap="orderMap">
        SELECT
            o.id,
            o.user_id,
            o.status,
            o.total_amount,
            o.order_date,
            o.created_at,
            o.updated_at,
            od.id AS detail_id,
            od.product_option_id,
            od.quantity,
            od.price,
            od.created_at AS detail_created_at,
            od.updated_at AS detail_updated_at
        FROM `ORDER` o
                 LEFT JOIN `ORDER_DETAIL` od ON (o.id = od.order_id)
        WHERE o.id = #{orderId} FOR UPDATE
    </select>

</mapper>